<Window x:Class="WpfAppMultiPatch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppMultiPatch"
        mc:Ignorable="d"
        Title="多开及防撤回补丁工具" Height="614" Width="350"
        WindowStartupLocation="CenterScreen"
        Background="#FFEAEAEA">
    <Window.Resources>
        <Style TargetType="Button" x:Key="MainButtonStyle">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF007ACC" Offset="0.0"/>
                        <GradientStop Color="#FF005C99" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FF0099E6" Offset="0.0"/>
                                            <GradientStop Color="#FF007ACC" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF004488"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PatchButtonStyle" BasedOn="{StaticResource MainButtonStyle}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF5CB85C" Offset="0.0"/>
                        <GradientStop Color="#FF4CAE4C" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FF6CD96C" Offset="0.0"/>
                                            <GradientStop Color="#FF5CB85C" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF398439"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <StackPanel Orientation="Vertical">

            <Label x:Name="VersionLabel" Content="" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="#FF333333"/>


           
            
            <!-- 4.1 栏 -->
         
            <Label Content="不打开微信、直接点多开补丁" FontSize="20" FontWeight="Bold" Margin="0,5,0,10" Foreground="#FF333333"/>
            <Button x:Name="BtnMultiInstancePatch41" Content="多开文件补丁 " Style="{StaticResource MainButtonStyle}" Click="BtnMultiInstancePatch41_Click"/>
           
                <Separator Margin="0,20" />
                <Label Content="登录界面执行，等提示后 正常登录" FontSize="15" FontWeight="Bold" Margin="0,5,0,10" Foreground="#FF333333"/>
                <Button x:Name="BtnRevoke" Content="打防撤回与DBKey补丁" Style="{StaticResource PatchButtonStyle}" Click="BtnRevoke_Click"/>
            <Separator Margin="0,20" />
            <Label x:Name="dbkey" Content="dbkey" FontSize="16" FontWeight="Bold" Margin="0,5,0,10" Foreground="#FF333333"/>

            <StackPanel Margin="10">
                <TextBox
    x:Name="infoText"
    Text="{Binding YourText}"
    FontSize="22"
    Foreground="#FF555555"
    TextWrapping="Wrap"
    IsReadOnly="True"
    BorderThickness="0"
    Background="Transparent"
    ContextMenu="{x:Null}" />


             
        
            </StackPanel>
            <StackPanel Margin="6">
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>